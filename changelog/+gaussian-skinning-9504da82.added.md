Add experimental `newton.geometry.GaussianSkinning` for repeated Gaussian assets with shared appearance arrays, independent GPU poses, updated local and scene bounds, and CUDA graph replay. Register bindings before constructing camera sensors; provide shape-local rest-to-current node transforms. Skinning is forward-only and preserves Gaussian scales rather than computing affine covariance deformation.
